class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        n = len(nums)
        for i in range(n):
            for j in range(n):
                if nums[i] == nums[j] and majority_element :
                    return i
                    

                    
            
        
        